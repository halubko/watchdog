[package]
name = "watchdog_scheduler"
version.workspace = true
edition.workspace = true

[lib]
name = "watchdog_scheduler"
path = "src/lib.rs"

[dependencies]
reqwest = "0.13.4"
tokio = { version = "1", features = ["full"] }
tracing = "0.1.44"

watchdog_core = { path = "../watchdog_core" }